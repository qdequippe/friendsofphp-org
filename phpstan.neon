includes:
    - vendor/symplify/phpstan-extensions/config/config.neon

parameters:
    level: max

    paths:
        - bin
        - src
        - tests
        - packages

    reportUnmatchedIgnoredErrors: false
